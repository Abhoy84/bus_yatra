class Pass {
  String name, pid, age, gender, orderid;
  Pass(
      {required this.name,
      required this.age,
      required this.gender,
      required this.orderid,
      required this.pid});
}
